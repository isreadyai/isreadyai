{
  "name": "@isreadyai/scanner",
  "version": "1.0.2",
  "description": "Runtime-agnostic engine that audits whether a site is ready for AI crawlers and LLM search engines.",
  "license": "MIT",
  "author": "Smart Squad S.r.l. (https://smartsquad.io)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./checks": "./src/checks/index.ts",
    "./providers": "./src/providers/index.ts"
  },
  "scripts": {
    "clean": "rm -rf .turbo .next .expo dist coverage *.tsbuildinfo",
    "test": "bun test",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "~6.0.3"
  }
}
